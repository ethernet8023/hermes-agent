name: Docs Site Checks

on:
  workflow_call:

permissions:
  contents: read

jobs:
  docs-site-checks:
    runs-on: arc-runner-set
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        id: npm-cache
        with:
          path: website/node_modules
          # node26: native builds + engine checks are node-major-specific;
          # keep in sync with NODE_MAJOR in the runner image (see
          # hermes-agent-ci-infra runner/Dockerfile).
          key: node-modules-cache-node26-${{ hashFiles('website/package-lock.json') }}

      - name: Install website dependencies
        uses: ./.github/actions/retry
        with:
          command: npm ci
          working-directory: website
        if: steps.npm-cache.outputs.cache-hit != 'true'

      - name: Install ascii-guard
        uses: ./.github/actions/retry
        with:
          command: uv pip install ascii-guard==2.3.0 pyyaml==6.0.3

      - name: Extract skill metadata for dashboard
        run: uv run website/scripts/extract-skills.py

      - name: Regenerate per-skill docs pages + catalogs
        run: uv run website/scripts/generate-skill-docs.py

      - name: Lint docs diagrams
        run: npm run lint:diagrams
        working-directory: website

      - name: Build Docusaurus
        uses: ./.github/actions/profile
        with:
          label: docs-site-build
          working-directory: website
          command: npm run build
