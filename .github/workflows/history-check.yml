name: History Check

# Rejects PRs whose branch has no common ancestor with main.
#
# In May 2026 PR #25045 was merged from a branch that had been disconnected
# from main's history (likely an accidental `git checkout --orphan` or
# `.git/` re-init).  GitHub's merge UI does not refuse merges of unrelated
# histories, so the PR landed cleanly with the intended one-file change —
# but its parent-less root commit (413990c94) got grafted into main as a
# second root, and ~1500 files' worth of `git blame` history collapsed
# onto that single commit.
#
# This check catches the failure mode by requiring `git merge-base` between
# the PR head and main to be non-empty.

on:
  workflow_call:
    outputs:
      review_status:
        description: "JSON array of review_status objects for the synthesizer."
        value: ${{ jobs.check-common-ancestor.outputs.review_status }}

permissions:
  contents: read

jobs:
  check-common-ancestor:
    # Short gate job: small runner, no dind (see hermes-agent-ci-infra).
    runs-on: arc-runner-small
    timeout-minutes: 10
    outputs:
      review_status: ${{ steps.merge-base-check.outputs.review_status }}
    steps:
      - name: Seed git objects from the node-local mirror
        # The GKE runner nodes keep a bare mirror of this repo on local disk
        # (hermes-agent-ci-infra k8s/git-mirror-daemonset.yaml). Pointing a fresh
        # .git at it via alternates makes the checkout below resolve nearly every
        # object locally — ~1MB fetched instead of ~61MB. That matters because ~40
        # pods check out at once and saturate the pool's egress.
        #
        # It matters doubly here: the one case that still needs full history
        # (proving no common ancestor exists) unshallows, and with the mirror
        # that history is already on local disk.
        #
        # actions/checkout is unmodified: it reuses an existing .git when the fetch
        # URL matches (git-directory-helper.ts prepareExistingDirectory).
        shell: bash
        run: |
          set -euo pipefail
          MIRROR=/mnt/git-mirror/hermes-agent.git
          if [ ! -d "$MIRROR/objects" ]; then
            echo "::notice::no node mirror, using a normal checkout"; exit 0
          fi
          git config --global --add safe.directory "$MIRROR"
          if ! git -C "$MIRROR" rev-parse --git-dir >/dev/null 2>&1; then
            echo "::warning::node mirror unreadable, using a normal checkout"; exit 0
          fi
          if [ -d "$GITHUB_WORKSPACE/.git" ]; then exit 0; fi
          git init --quiet "$GITHUB_WORKSPACE"
          echo "$MIRROR/objects" > "$GITHUB_WORKSPACE/.git/objects/info/alternates"
          echo "::notice::seeded git alternates from $MIRROR"

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          # No fetch-depth: 0. The merge-base action below deepens only as far
          # as it takes to connect the two histories (~15s, against 76-81s to
          # fetch all ~1400 refs). It still unshallows in the one case that
          # genuinely needs full history — proving NO common ancestor exists,
          # which is exactly the branch this job rejects.
          fetch-depth: 1

      - name: Resolve merge base
        id: mb
        uses: ./.github/actions/merge-base
        with:
          base: ${{ github.event.pull_request.base.sha }}
          head: ${{ github.event.pull_request.head.sha }}
          # Absence is the thing this job measures — report it below, in our
          # own words, instead of dying with the action's generic error.
          fail-on-missing: 'false'

      - id: merge-base-check
        name: Reject PRs with no common ancestor on main
        env:
          FOUND: ${{ steps.mb.outputs.found }}
          BASE: ${{ steps.mb.outputs.sha }}
        run: |
          if [ "$FOUND" != "true" ]; then
            STATUS='[{"source":"unrelated histories","results":[{"kind":"action_required","title":"Unrelated histories","summary":"This PR has no common ancestor with main.","detail":"","how_to_fix":"Rebase your changes onto current main:\n```\ngit fetch origin main\ngit checkout -b fix-branch origin/main\n# re-apply your changes (cherry-pick, copy files, etc.)\ngit push -f origin fix-branch\n```\n"}]}]'
            echo "review_status=${STATUS}" >> "$GITHUB_OUTPUT"
            echo ""
            echo "::error::This PR has no common ancestor with main."
            echo ""
            echo "Your branch's history is disconnected from main.  Common causes:"
            echo "  - the branch was created with 'git checkout --orphan'"
            echo "  - '.git/' was re-initialized at some point during the work"
            echo "  - the branch was force-pushed from an unrelated repository"
            echo ""
            echo "Merging an unrelated-history PR grafts a parent-less root commit"
            echo "into main and collapses git blame for every file in that snapshot."
            echo "Reference: PR #25045 caused this and re-rooted blame on ~1500"
            echo "files to a single orphan commit."
            echo ""
            echo "To fix, rebase your changes onto current main:"
            echo "  git fetch origin main"
            echo "  git checkout -b fix-branch origin/main"
            echo "  # re-apply your changes (cherry-pick, copy files, etc.)"
            echo "  git push -f origin fix-branch"
            exit 1
          fi
          echo "::notice::Common ancestor with main: $BASE"
          echo "review_status=[]" >> "$GITHUB_OUTPUT"
