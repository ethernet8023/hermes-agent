name: Installer tests

# scripts/install.ps1's PowerShell smoke tests: the stage protocol the
# desktop bootstrapper (Hermes-Setup) drives. Windows PowerShell 5.1 as well
# as pwsh 7: install.ps1 is delivered via `irm | iex` into whatever shell the
# user already has, and 5.1 is what ships with Windows.

on:
  workflow_call:

permissions:
  contents: read

concurrency:
  group: installer-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  powershell:
    name: PowerShell installer tests
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Stage protocol (pwsh 7)
        shell: pwsh
        run: pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/tests/test-install-ps1-stage-protocol.ps1

      - name: Stage protocol (Windows PowerShell 5.1)
        shell: powershell
        run: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/tests/test-install-ps1-stage-protocol.ps1
